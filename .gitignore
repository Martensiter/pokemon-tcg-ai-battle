# --- DO NOT SHIP: Pokemon Competition Data (rights belong to Pokemon) ---
# Per competition rules, these must NOT be redistributed publicly.
EN_Card_Data.csv
JP_Card_Data.csv
cg/cg.dll
cg/libcg.so
sample_submission/
episodes/
episodes_*/
deck_sample.csv

# --- Backups and intermediate artifacts ---
agent/weights_v*.npz
# Distillation policy net: a local-only trained artifact (opt-in via POLICY_PUCT_C).
# Keep it out of git until verified; make_submission still bundles a local copy.
# Force-add (git add -f agent/policy.npz) only to ship a verified policy.
agent/policy.npz
policy_data*.npz
value_data*.npz
selfplay/data*.npz
*.npz.bak

# --- Submission bundles (regenerable) ---
submission/
submission_*/
submission_*.zip
submission_*.tar.gz
replay.html

# --- Plans / scratch ---
plans/
review_cabt/
_review_*/

# --- Python ---
__pycache__/
*.py[cod]
*$py.class
*.so
*.egg-info/
.venv/
venv/
env/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# --- IDE / OS ---
.vscode/
.idea/
*.swp
.DS_Store
Thumbs.db

# --- Logs / temp ---
*.log
/tmp/

# --- Collector: secrets, raw data, intermediate outputs (NEVER commit) ---
.env
kaggle.json
collector_data/
collector_state/
# but DO keep the Kaggle Dataset metadata template under collector/
!collector/dataset-metadata.json
*.npz.zip
value_chunks.zip
collector.out
